#include <stdio.h>
int main() {
    int n, sum=0;
    printf("Enter size: ");
    scanf("%d", &n);
    int arr[n];
    for(int i=0; i<n; i++) scanf("%d", &arr[i]);
    for(int i=0; i<n; i++) sum += arr[i];
    printf("Average = %.2f\n", (float)sum/n);
    return 0;
}
